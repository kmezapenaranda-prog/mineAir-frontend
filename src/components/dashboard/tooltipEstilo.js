/**
 * Clase compartida para los tooltips "materializados" (vidrio + blur) de
 * todas las gráficas del dashboard — mismo lenguaje visual que TooltipCh4
 * en vez de que cada gráfica reinvente su propio panel flotante.
 */
export const TOOLTIP_CLASS =
  'rounded-xl border border-border bg-surface-raised/95 p-3 text-xs shadow-2xl backdrop-blur'
